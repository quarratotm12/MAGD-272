using UnityEngine;
using System.Collections;

public class DestroyObject : MonoBehaviour {

	// Use this for initialization
	public void destroyMusic (AudioSource music) {
		Destroy (music);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
